export interface GuildConfig {
  guild: string;
  realm: string;
  region: string;
}

export const guildConfig: GuildConfig = {
  guild: "Distinctly Average",
  realm: "Silvermoon",
  region: "EU"
};
